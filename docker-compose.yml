version: "2.1"
services:
  mysqldb:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=
      - MYSQL_ALLOW_EMPTY_PASSWORD=1
      - MYSQL_DATABASE=alinfo
    ports:
      - 3306:3306
    volumes:
      - './.mysql-data/db:/var/lib/mysql'
