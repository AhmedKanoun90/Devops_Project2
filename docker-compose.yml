version: '2.2'
services: 
  database:
    image: phpmyadmin/phpmyadmin
    restart: always 
    ports:
      - "8080:80"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tpachato
   
